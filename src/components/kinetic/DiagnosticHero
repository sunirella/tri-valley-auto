import React from "react";
import { motion } from "framer-motion";
import { Link } from "react-router-dom";
import { ArrowRight, Star, Phone } from "lucide-react";
import RapidIntake from "./RapidIntake";

const HERO_IMG = "https://media.base44.com/images/public/69e3f56eb6ba73e7f55e72bb/9bb2d454f_generated_image.png";

export default function DiagnosticHero() {
  return (
    <section className="relative w-full bg-background overflow-hidden pt-16">

      {/* Full-bleed background image with warm overlay */}
      <div className="absolute inset-0 z-0">
        <img src={HERO_IMG} alt="Auto service" className="w-full h-full object-cover object-center" />
        <div className="absolute inset-0 bg-gradient-to-r from-background via-background/85 to-background/30" />
        <div className="absolute inset-0 bg-gradient-to-t from-background via-transparent to-transparent" />
      </div>

      <div className="relative z-10 max-w-[1440px] mx-auto px-5 md:px-10 lg:px-16 pt-14 pb-0 min-h-[92vh] flex flex-col justify-center">

        {/* Eyebrow */}
        <motion.div
          initial={{ opacity: 0, y: 10 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.5 }}
          className="flex items-center gap-3 mb-6"
        >
          <span className="live-dot" />
          <span className="text-micro text-accent">Open Now · Closes 5 PM · Pleasanton, CA</span>
          <span className="hidden md:flex items-center gap-1 ml-4">
            {[...Array(5)].map((_, i) => (
              <Star key={i} className="w-3.5 h-3.5 fill-highlight text-highlight" />
            ))}
            <span className="text-micro text-muted-foreground ml-1">4.4 (28 reviews)</span>
          </span>
        </motion.div>

        {/* Main heading — GIANT */}
        <motion.h1
          initial={{ opacity: 0, y: 30 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.65, ease: [0.16, 1, 0.3, 1] }}
          className="font-display font-black uppercase leading-[0.85] tracking-tight"
          style={{ fontSize: "clamp(4rem, 14vw, 13rem)" }}
        >
          YOUR CAR<br />
          <span className="text-accent italic">FIXED</span><br />
          RIGHT.
        </motion.h1>

        {/* Sub */}
        <motion.p
          initial={{ opacity: 0, y: 16 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.5, delay: 0.25 }}
          className="mt-8 text-lg md:text-xl text-foreground/75 max-w-lg leading-relaxed font-body"
        >
          Owner-operated auto repair in Pleasanton. Engine diagnostics, smog inspections,
          European vehicles — done by Ron, done right, done fast.
        </motion.p>

        {/* CTAs */}
        <motion.div
          initial={{ opacity: 0, y: 12 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ duration: 0.45, delay: 0.38 }}
          className="flex flex-wrap items-center gap-4 mt-10"
        >
          <Link to="/book"
            className="group inline-flex items-center gap-3 bg-accent text-accent-foreground px-8 py-4 font-display font-bold text-xl uppercase hover:bg-highlight transition-colors duration-200">
            BOOK NOW
            <ArrowRight className="w-5 h-5 transition-transform group-hover:translate-x-1" />
          </Link>
          <a href="tel:+19256008975"
            className="inline-flex items-center gap-3 border border-foreground/30 text-foreground px-8 py-4 font-display font-bold text-xl uppercase hover:border-accent hover:text-accent transition-colors duration-200">
            <Phone className="w-5 h-5" />
            CALL US
          </a>
        </motion.div>

        {/* Trust bar */}
        <motion.div
          initial={{ opacity: 0 }}
          animate={{ opacity: 1 }}
          transition={{ delay: 0.55, duration: 0.5 }}
          className="flex flex-wrap items-center gap-6 md:gap-10 mt-14 pt-8 border-t border-hairline"
        >
          {[
            { v: "4.4 ★", l: "Google Rating" },
            { v: "28",    l: "Verified Reviews" },
            { v: "$79",   l: "Smog Inspections" },
            { v: "25% OFF", l: "Oil Changes" },
          ].map((m) => (
            <div key={m.l}>
              <div className="font-display font-black text-3xl md:text-4xl uppercase text-foreground">{m.v}</div>
              <div className="text-micro text-muted-foreground mt-1">{m.l}</div>
            </div>
          ))}
        </motion.div>
      </div>

      {/* Rapid intake bar */}
      <div className="relative z-10 mt-14">
        <RapidIntake />
      </div>
    </section>
  );
}
