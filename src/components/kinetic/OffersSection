import React from "react";
import { Link } from "react-router-dom";
import { ArrowRight, Zap } from "lucide-react";

const OFFERS = [
  {
    badge: "This Month Only",
    discount: "25% OFF",
    title: "Oil Change",
    desc: "Full synthetic or conventional. Filter included. Any make or model.",
    cta: "Claim Offer",
    hot: true,
  },
  {
    badge: "Always",
    discount: "$79",
    title: "Smog Inspection",
    desc: "California STAR-certified. Certificate included. In and out in 30 minutes.",
    cta: "Book Smog",
    hot: false,
  },
  {
    badge: "Limited Time",
    discount: "FREE",
    title: "Diagnostic Scan",
    desc: "Free OBD-II diagnostic with any paid repair. Know exactly what's wrong.",
    cta: "Schedule Now",
    hot: false,
  },
  {
    badge: "New Customers",
    discount: "$25 OFF",
    title: "First Service",
    desc: "First-time customers get $25 off any repair or service over $100.",
    cta: "Claim Deal",
    hot: false,
  },
];

export default function OffersSection() {
  return (
    <section id="offers" className="bg-background border-t border-hairline">
      {/* Section title bar */}
      <div className="bg-accent">
        <div className="max-w-[1440px] mx-auto px-5 md:px-10 lg:px-16 py-8 md:py-10 flex flex-col md:flex-row md:items-end md:justify-between gap-4">
          <div>
            <div className="text-micro text-accent-foreground/70 mb-2">Limited Time Deals</div>
            <h2 className="font-display font-black uppercase text-accent-foreground leading-none"
              style={{ fontSize: "clamp(2.5rem, 7vw, 6rem)" }}>
              CURRENT OFFERS
            </h2>
          </div>
          <p className="text-base text-accent-foreground/80 max-w-xs font-body">
            Real savings, no fine print. Book online or call to lock in your rate.
          </p>
        </div>
      </div>

      {/* Offer cards */}
      <div className="max-w-[1440px] mx-auto px-5 md:px-10 lg:px-16 py-0">
        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 border-l border-hairline">
          {OFFERS.map((offer) => (
            <div key={offer.title}
              className={`relative border-r border-b border-hairline p-8 md:p-10 flex flex-col group ${offer.hot ? "bg-card" : "bg-background"}`}>
              {/* Hot badge */}
              {offer.hot && (
                <div className="absolute top-0 right-0 bg-accent text-accent-foreground text-micro px-3 py-1.5 flex items-center gap-1.5">
                  <Zap className="w-3 h-3" />
                  POPULAR
                </div>
              )}

              <div className="text-micro text-muted-foreground mb-5">{offer.badge}</div>

              {/* Discount */}
              <div className="font-display font-black uppercase leading-none mb-3"
                style={{ fontSize: "clamp(3.5rem, 7vw, 5.5rem)", color: offer.hot ? "hsl(var(--accent))" : "hsl(var(--foreground))" }}>
                {offer.discount}
              </div>

              <div className="font-display font-bold text-2xl uppercase text-foreground mb-3">
                {offer.title}
              </div>

              <p className="text-[14px] text-muted-foreground leading-relaxed font-body flex-1">
                {offer.desc}
              </p>

              <Link to="/book"
                className="mt-8 inline-flex items-center gap-2 font-display font-bold text-base uppercase text-accent hover:gap-4 transition-all duration-200">
                {offer.cta}
                <ArrowRight className="w-4 h-4" />
              </Link>
            </div>
          ))}
        </div>
      </div>
    </section>
  );
}
